#include <iostream>
#include <vector>

using namespace std;

class Solution {
public:
    vector<int> getConcatenation(vector<int>& nums) {
        vector<int> ans;

        ans.resize
    }
};

int main() {
    auto s = Solution();

    cout << s.getConcatenation(1, 1) << '\n'; //213
}
