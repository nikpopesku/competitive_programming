#include <vector>

using namespace std;

class Solution {
public:
    int mctFromLeafValues(vector<int> &arr) {
    }
};


int main() {
    auto s = Solution();

    vector<int> arr = {6, 2, 4};
    cout << s.mctFromLeafValues(arr) << endl; // 32
}
