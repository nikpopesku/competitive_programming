#include <iostream>
#include <stack>
#include <vector>

using namespace std;

void solve() {
    int N;
    cin >> N;
    vector v(N, vector<int>(N));
}

int main() {
    int t;
    cin >> t;
    while (t--) {
        solve();
    }
}
