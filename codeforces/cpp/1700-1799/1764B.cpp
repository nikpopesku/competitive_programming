#include <bits/stdc++.h>

using namespace std;

int main() {
    int t;
    cin >> t;
    while (t--) {
        int n, value;
        cin >> n;

        vector<int> vc;

        for (int i = 0; i < n ; ++i) {
            cin >> value;
            vc.push_back(value);
        }

        cout << 1 << "\n";
    }
}