#include <bits/stdc++.h>

using namespace std;

#define int long long

void solve()
{
    int n, k, q, l, r;
    cin >> n >> k >> q;
    vector<int> a(n);
    for (int i = 0; i < n; ++i)
    {
        cin >> a[i];
    }

    for (int i = 0; i < q; ++i)
    {
        cin >> l >> r;
    }
}

signed main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);
    int t;
    cin >> t;
    while (t--) solve();
}
