#include <iostream>
#include <vector>
#include <map>

int main() {
    int t;
    int n, m;
    std::cin >> t;

    for (long long i = 0; i < t; i++) {
        std::cin >> n;


        for (long long j = 0; j < n; j++) {
            std::cin >> n >> m;

        }
    }

    return 0;
}