#include <iostream>
#include <vector>
#include <map>

int main() {
    int t;
    int n, m;
    int a, b, d;
    std::cin >> t;

    for (auto i = 0; i < t; i++) {
        std::cin >> n;


        for (auto j = 0; j < n; j++) {
            std::cin >> n >> m;

            for (auto k = 0; k < m; k++) {
                std::cin >> a >> b >> d;
            }
        }
    }

    return 0;
}