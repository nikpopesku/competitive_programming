#include <iostream>
#include <vector>

using namespace std;

#define ll long long

constexpr ll MOD = 1e9 + 7;

void solve()
{
    ll n, k;
    cin >> n >> k;
    vector<ll> v(n);

    for (ll i = 0; i < n; ++i)
    {
        cin >> v[i];
    }
}

int main()
{
    int t;
    cin >> t;
    while (t--)
    {
        solve();
    }
}
