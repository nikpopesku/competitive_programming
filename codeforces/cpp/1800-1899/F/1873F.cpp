#include <iostream>
#include <vector>

#define ll long long

using namespace std;

void solve()
{
    int n, k;
    cin >> n >> k;

    vector<int> a(n);
    vector<int> h(n);

    for (int i = 0; i < n; ++i) cin >> a[i];
    for (int i = 0; i < n; ++i) cin >> h[i];


}

int main()
{
    int t;
    cin >> t;

    while (t--)
    {
        solve();
    }
}
