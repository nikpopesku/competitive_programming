#include<iostream>
#include <vector>

using namespace std;

int main() {
    int N, X;
    int A[50], B[50];

    cin >> N >> X;
    for (int i = 0 ; i < N; ++i) {
        cin >> A[i] >> B[i];
    }
}
