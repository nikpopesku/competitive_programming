#include <iostream>
#include <vector>

using namespace std;

#define ll long long

int main() {
    ll n, k, x;
    cin >> n >> k >> x;

    vector<ll> vc(n);

    for (auto i = 0; i < n; ++i) {
        cin >> vc[i];
    }
}