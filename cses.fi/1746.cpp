#include<bits/stdc++.h>

using namespace std;

#define ll long long

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);

    const int MOD = 1e9 + 7;
    int n, m;
    cin >> n >> m;
    vector<ll> num(n);
    for (int i = 0; i < n; ++i) cin >> num[i];

    vector<vector<ll>> vc(n, vector<ll>(m+1, 0));


}